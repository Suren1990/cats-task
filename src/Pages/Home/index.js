import styles from './Home.module.scss';

const Home = () => {
    return (
        <section className={styles.home}>
            <h1 className={styles.home__title}>This is the Home Page</h1>
        </section>
    );
};

export default Home;
